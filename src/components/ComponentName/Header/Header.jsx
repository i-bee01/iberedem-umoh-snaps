import {useState} from "react";
import "./Header.scss";
// import FilterButton from "../FilterButton.jsx";

const Header = ({
    selectedFilters,
    onFilterToggle,
    isFilterOpen,
    onFilterButtonClick,
}) => {
    return(
        <div className="header">Snaps</div>

    )
}

export default Header;